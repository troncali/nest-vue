/** GraphQL configuration properties for intellisense. */
interface GqlOptionTypes {
	// Full property path is required for nested properties.
	"gql.autoSchemaFile": boolean;
	"gql.sortSchema": boolean;
}

export { GqlOptionTypes };
